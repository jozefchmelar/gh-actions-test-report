name: 'Artifacts'
on:
  workflow_dispatch:
jobs:
  artifacts:
    runs-on: windows-latest
    steps:
    - uses: actions/download-artifact@master
      with:
        name: artifacts
        path: nugets/
    - run: ls nugets
